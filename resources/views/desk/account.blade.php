@extends('layouts.desk')
@section('content')<div class="desk-heading"><div><h1>Pengaturan akun</h1><p>{{ auth()->user()->name }} · {{ auth()->user()->email }}</p></div></div><form class="panel stack form-width" method="post" action="{{ route('password.change') }}">@csrf<h2>Ganti kata sandi</h2><label>Kata sandi saat ini<input type="password" name="current_password" required autocomplete="current-password"></label><label>Kata sandi baru<input type="password" name="password" minlength="12" required autocomplete="new-password"></label><label>Ulangi kata sandi baru<input type="password" name="password_confirmation" required autocomplete="new-password"></label><button class="button">Perbarui kata sandi</button></form><form class="submit-row" method="post" action="{{ route('logout') }}">@csrf<button class="button outline">Keluar dari akun</button></form>@endsection
