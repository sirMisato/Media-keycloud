@extends('layouts.desk')
@section('content')<div class="desk-heading"><div><span class="eyebrow">KURASI & VERIFIKASI</span><h1>Antrean redaksi</h1><p>Tinjau substansi, rujukan, dan bahasa sebelum memberi persetujuan.</p></div></div><section class="panel"><div class="table-scroll"><table><thead><tr><th>Naskah</th><th>Penulis</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($revisions as $r)<tr><td><strong>{{ $r->title }}</strong><small>{{ $r->category->name }}</small></td><td>{{ $r->article->author->name }}</td><td><span class="status {{ $r->status }}">{{ $r->label() }}</span>@if($r->publish_at)<small>{{ $r->publish_at->format('d/m/Y H:i') }} WIB</small>@endif</td><td><a href="{{ route('preview',$r) }}">Periksa naskah →</a></td></tr>@empty<tr><td colspan="4" class="empty-state">Antrean kosong. Naskah yang dikirim kontributor akan muncul di sini.</td></tr>@endforelse</tbody></table></div>{{ $revisions->links() }}</section>@endsection
