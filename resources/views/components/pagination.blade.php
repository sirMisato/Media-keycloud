@if($paginator->hasPages())<nav class="pagination" aria-label="Navigasi halaman">@if($paginator->onFirstPage())<span>Sebelumnya</span>@else<a href="{{ $paginator->previousPageUrl() }}" rel="prev">← Sebelumnya</a>@endif<span>{{ $paginator->currentPage() }} / {{ $paginator->lastPage() }}</span>@if($paginator->hasMorePages())<a href="{{ $paginator->nextPageUrl() }}" rel="next">Berikutnya →</a>@else<span>Berikutnya</span>@endif</nav>@endif
